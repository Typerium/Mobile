import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  scroll: {
    width: '100%',
  },
  scrollContent: {
    flexGrow: 1,
    alignItems: 'center',
    padding: 2,
  },
});
